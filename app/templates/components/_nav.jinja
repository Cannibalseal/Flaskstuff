<header class="site-header card" role="banner">
  <div style="display:flex;align-items:center;justify-content:space-between;gap:1rem;">
    <div>
      <h1 style="margin:0; font-size:1.5rem;"><a href="{{ url_for('public.index') }}" style="text-decoration:none;">Sealy's Flask Blog</a></h1>
      <div style="color: var(--cyan); font-size:0.95rem; opacity: 0.9; margin-top: 0.25rem;">Small projects and notes</div>
    </div>
    <nav aria-label="Main navigation">
      <a class="button" href="{{ url_for('public.about') }}">About</a>
      <a class="button" href="{{ url_for('public.articles') }}">Articles</a>
      <a class="button" href="{{ url_for('admin.dashboard') }}">Admin</a>
      {% if session.get('logged_in') %}
        <a class="button" href="{{ url_for('auth.logout') }}">Logout</a>
      {% endif %}
    </nav>
  </div>
</header>
