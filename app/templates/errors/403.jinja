{% extends "components/_main.jinja" %}

{% block main %}
<div class="error-container">
    <div class="error-code">403</div>
    <div class="error-title">🚫 Access Denied: VIP Only!</div>
    <div class="error-message">
        <p>Sorry, but this area is more exclusive than a secret society of cats. 🐱👑</p>
        <p class="error-detail">You need special permissions to enter this digital fortress.</p>
    </div>
    
    <div class="error-suggestions">
        <h3>What now?</h3>
        <ul>
            <li>🔐 <a href="{{ url_for('auth.login') }}">Log in</a> if you have credentials</li>
            <li>🏠 Go back to the <a href="{{ url_for('public.index') }}">homepage</a></li>
            <li>📧 Contact the admin (nicely) for access</li>
            <li>🎩 Try wearing a monocle - might make you look more important</li>
        </ul>
    </div>
    
    <div class="error-ascii">
        <pre>
    ┌───────┐
    │ NOPE! │
    └───────┘
      (ಠ_ಠ)
        </pre>
    </div>
</div>
{% endblock %}
