<header class="site-header card" role="banner">
  <div style="display:flex;align-items:center;justify-content:space-between;gap:1rem;">
    <div>
      <h1 style="margin:0; font-size:1.25rem;"><a href="{{ url_for('index') }}">Sealy's Flask Blog</a></h1>
      <div class="muted">Small projects and notes</div>
    </div>
    <nav aria-label="Main navigation">
      <a class="button" href="{{ url_for('about') }}">About</a>
      <a class="button" href="{{ url_for('articles') }}">Articles</a>
      <a class="button" href="{{ url_for('view_admin') }}">Admin</a>
      {% if session.get('logged_in') %}
        <a class="button" href="{{ url_for('view_logout') }}">Logout</a>
      {% endif %}
    </nav>
  </div>
</header>
